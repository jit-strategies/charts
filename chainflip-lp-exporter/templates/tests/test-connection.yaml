apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chainflip-lp-api.fullname" . }}-test-connection"
  labels:
    {{- include "chainflip-lp-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chainflip-lp-api.fullname" . }}:{{ .Values.httpPort }}']
  restartPolicy: Never
