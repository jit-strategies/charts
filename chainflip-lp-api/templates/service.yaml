apiVersion: v1
kind: Service
metadata:
  name: {{ include "chainflip-lp-api.fullname" . }}
  labels:
    {{- include "chainflip-lp-api.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "chainflip-lp-api.selectorLabels" . | nindent 4 }}
